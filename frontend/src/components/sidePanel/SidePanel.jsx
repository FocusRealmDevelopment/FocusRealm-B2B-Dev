// import React from 'react';
import styles from "./sidepanel.module.css";

const SidePanel = () => {
  return <div className={`${styles.body}`}>This is the Side Panel.</div>;
};

export default SidePanel;
