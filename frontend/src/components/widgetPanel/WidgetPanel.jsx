// import React from 'react';
import styles from "./widgetpanel.module.css";

const WidgetPanel = () => {
  return <div className={`${styles.body}`}>This is the Widget Panel.</div>;
};

export default WidgetPanel;
